# Linux packaging (placeholder)

Future home for .deb / .rpm / AppImage wrappers around
`python -m aipotluck.installer.install`. Not implemented yet -- the systemd service
backend (`aipotluck/installer/service/systemd.py`) is implemented and tested directly
against systemd --user on this repo's dev host.
