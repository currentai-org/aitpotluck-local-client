# Linux packaging (placeholder)

Future home for .deb / .rpm / AppImage wrappers around
`python -m installer.install`. Not implemented yet -- the systemd service
backend (`installer/service/systemd.py`) is implemented and tested directly
against systemd --user on this repo's dev host.
