# Windows packaging (placeholder)

Future home for a native installer wrapper (Inno Setup / WiX MSI) around
`python -m installer.install`. Not implemented yet -- this phase ships the
Python installer directly; a double-click installer is later work.

Also the eventual location for pywin32-based Windows Service registration
docs/scripts once validated on real Windows hardware (see
`installer/service/windows_service.py`, currently stubbed and untested).
